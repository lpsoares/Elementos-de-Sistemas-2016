// by Luciano Soares.
// File name: projects/03/a/Register32.hdl

/**
 * 32-bit register:
 * If load[t] == 1 then out[t+1] = in[t]
 * else out does not change
 */

CHIP Register {
    IN in[32], load;
    OUT out[32];

    PARTS:
    // Put your code here:
}
